import "package:hane/utils/UnitService.dart";
import "package:hane/utils/UnitValidator.dart";

class FireStoreParser {
  // static ({double amount, String unit}) parseConcentration(String concentration) {
  //   List<String> parts = concentration.split(' ');
  //   if (parts.length != 2 ) {
  //     throw Exception("Not a valid concentration, should be in the form of 'amount unit'");
  //   }
  //   if (UnitValidator.isValidConcentrationUnit(concentration)[1])
  //   return ({amount: double.parse(parts[0]), unit: parts[1]});
  // }
  // return (amount: map['amount'], unit: map['unit']);
  }
